name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API docs
        run: curl -fsS https://quickbite-bfwl.onrender.com/docs > /dev/null
